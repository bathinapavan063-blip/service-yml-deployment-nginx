apiVersion: v1
kind: Service
metadata:
 name: nginx-service
spec:
 type: LoadBalancer
 ports:
  - name: http
    protocol: TCP
    targetPort: 80   
    port: 80        
 selector:
  name: web-app
  type: frontend 
